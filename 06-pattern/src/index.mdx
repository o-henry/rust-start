## 열거자와 패턴매칭

- 열거자는 사용 가능한 값만 나열한 타입을 정의할때 사용
- 러스트에는 null 개념이 존재하지 않는다.
- null은 현재 유효하지 않거나 혹은 어떤 이유로 존재하지 않는 값을 표현하려 할때 사용.
- 널값의 문제는 널값을 널이 아닌 값처럼 사용하려고 하면 에러가 발생하는 것.
- 러스트는 어떤 값의 존재 여부를 표현하는 열거자를 정의하고 있다. ( Option )
