### 러스트에서 기본 변수는 불변성 이며, 안전성과 손쉬운 동시성 이라는 장점을 갖는다.

- 데이터 규모가 작을수록 새 인스턴스를 생성하고, 함수적 프로그래밍 스타일로 작성하는 것이 합리적이다.
- 데이터 규모가 클수록 가변 인스턴스를 사용하는 것이 빠를 수 있다.
- 반드시 값의 유형을 선언해야 한다.
- `mut` 을 통해 가변변수를 정의할 수 있다.
- `const`는 불변성 그 자체 이며 mut 사용이 불가능하다.
